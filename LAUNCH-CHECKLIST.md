# 루미(lumi) 런칭 체크리스트 — 2026-06-10 갱신

오픈 전 확인용. ✅=완료(검증됨) · ⬜=사장님 할 일.

---

## 🚦 오픈 전 필수 — 이 2개만 되면 오픈 가능

### ⬜ 1. OpenAI 키 충전 확인
- 캡션 생성(사진 분석 gpt-4o + 글쓰기)에 **OpenAI 키가 필수**입니다. (트렌드는 OpenAI 0원이라 무관)
- 확인: platform.openai.com → **Billing** 에 잔액/결제수단 있는지.
- 키가 죽어 있으면 캡션이 "실패" 상태로 뜹니다. (쿼터 한도는 첫 캡션 안 막음 — 점검 완료)

### ⬜ 2. 초안 모드 사진 1장 테스트 (코어 기능 첫 실작동)
지금까지 사진→캡션이 **실제로 한 번도 안 돌아봤습니다**(코드는 전부 검증됨). 한 번 돌려보세요:
1. lumi.it.kr 로그인 → **등록(register)** 페이지
2. **"초안만"** 버튼 선택 (또는 주소 `/register-product?draft=1`)
3. 사진 1장 올리고 제출
4. 잠시 후 **history(이력)** 페이지에서 캡션 생성 확인
5. **"📋 캡션 복사"** 눌러 복사 → 인스타에 직접 붙여넣기

확인 포인트:
- [ ] 캡션이 생성됨 (실패 아님)
- [ ] 캡션 내용이 사진과 맞음
- [ ] 복사 버튼 작동

> 초안 모드는 **게시를 안 합니다** — Meta 승인 없이도 캡션만 만들어 사장님이 직접 올리는 방식. 그래서 지금 바로 런칭 가능한 유일한 경로입니다.

---

## ✅ 이미 완료 (검증됨)
- **트렌드**: 8개 카테고리 깨끗·업종적합, OpenAI 0원, 매일 자동 갱신 (네이티브 자정 크론까지 검증)
- **보안**: 어드바이저 경고 35건 해소 + 엔드포인트 78개 전수 감사 (`security-hardening.sql` 적용 완료)
- **버그감사**: 전 함수 멀티에이전트 감사 — scheduler 26일 잠수 수습 포함 실버그 8+α건 수정 (`9322af4`·`9c71849`)
- **감시망**: 스케줄 크론 13개 전체 heartbeat + 워치독 전수 감시 — 크론 죽으면 메일 경보 (`84ce073`, 프로덕션 검증)
- **법적 페이지**: privacy/terms 실제 코드와 전수 대조 — 쓰레드·틱톡(조건부)·국외이전 보완, Meta·TikTok 심사 대비 (`6f90aa6`)
- **프런트 크리티컬 패스**: 가입→온보딩→초안 정밀 점검 — CSP로 깨져 있던 히스토리 썸네일, 초안 리다이렉트 탭, 로그아웃 갱신토큰 잔존 수정 (`e7f81a6`)
- **품질**: 토스 UX라이팅 카피, 그라데이션 가독성, 캡션 트렌드매칭 버그 수정
- **초안 모드 코드 경로**: reserve→캡션생성→history 전부 검증 (런타임만 미확인 = 위 2번)

---

## 🔧 초안 테스트가 실패하면
- **캡션이 "실패"로 뜸** → OpenAI 키 문제 1순위. 원인은 `reservations.caption_error` 컬럼에 기록됨 (Supabase → Table Editor → reservations → 해당 행).
- **전체 진단** → lumi.it.kr/api/cron-health 열어보기.
- **상세 로그** → Netlify 대시보드 → Functions → `process-and-post-background` 로그.
- 막히면 증상(+caption_error 값)을 어시스턴트한테 그대로 전달 → 같이 디버깅.

---

## 🟢 선택 (급하지 않음)
- **외부 업타임 모니터** (UptimeRobot 등 무료, 5분 설정) — 워치독은 Netlify 안에서 돌아서 Netlify 자체 장애는 못 잡음. https://lumi.it.kr/api/cron-health 를 5분 간격 핑하면 마지막 안전망 완성. 계정 생성 필요 = 사장님 몫.
- `performance-rls.sql` 실행 — RLS 성능 최적화. 0명 규모라 지금 효과 0, 트래픽 생긴 후 실행.
- Supabase **Leaked password protection** 토글 — Pro 플랜 전용. 무료면 스킵 OK(보안 구멍 아님).

---

## 되돌리기 (롤백)
- 배포 = `git push origin main` (이게 빌드 트리거, ⚠️ 강제 `/builds` API 금지).
- 문제 생기면 `git revert <커밋해시> && git push origin main` 로 즉시 롤백.
- 최근 주요 커밋: `c4b2373`(보안 게이트) · `a2986f4`(트렌드 시드) · `acfc812`(캡션 버그).
